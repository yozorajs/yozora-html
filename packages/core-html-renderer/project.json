{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "core-html-renderer",
  "sourceRoot": "packages/core-html-renderer/src",
  "projectType": "library",
  "tags": [],
  "targets": {
    "clean": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "packages/core-html-renderer",
        "parallel": false,
        "commands": ["rimraf lib"]
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "dependsOn": ["clean", "^build"],
      "options": {
        "cwd": "packages/core-html-renderer",
        "parallel": false,
        "sourcemap": true,
        "commands": ["rollup -c ../../rollup.config.mjs"]
      },
      "configurations": {
        "production": {
          "sourcemap": false,
          "env": {
            "NODE_ENV": "production"
          }
        }
      }
    },
    "watch": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "packages/core-html-renderer",
        "parallel": false,
        "sourcemap": true,
        "commands": ["rollup -c ../../rollup.config.mjs -w"],
        "env": {
          "NODE_ENV": "development"
        }
      }
    }
  }
}
